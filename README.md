### Waste Journeys development

Developed for Getting to Zero series by OHNY.

### Development

Clone the repo, then run `npm install` and `bower install`.

To test locally run `gulp serve`. To build for deploy run `gulp`.
